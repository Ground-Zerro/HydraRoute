# HydraRoute Neo

---

## 📚 Оглавление

- [🚀 Возможности](#-возможности)
- [📋 Системные требования](#-системные-требования)
- [💾 Установка и обновление](#-установка-и-обновление)
- [📁 Работа с доменами](#-работа-с-доменами)
- [🔧 Политики доступа](#-политики-доступа)
- [⚙️ Технические детали](#️-технические-детали)
- [🔄 Управление](#-управление)
- [🌐 IPv6](#-ipv6)
- [❌ Удаление](#-удаление)
- [ℹ️ Примечания и лицензия](#️-примечания-и-лицензия)

---

## 🚀 Возможности

### Маршрутизация и производительность
- Не требует отключения системного DNS-сервера роутера
- Нет "посредников" между устройством и DNS сервером
- Работает без зависимости от AdGuard Home
- Минимальное потребление ресурсов
- Количество политик доступа не ограничено
- Прямая маршрутизация в интерфейс

### Поддержка современных прокси-протоколов xRay
- **VLESS** с транспортами TCP, WS, gRPC, HTTPUpgrade, SplitHTTP (XHTTP)
- **VMess** с транспортами TCP, WS, gRPC, HTTPUpgrade, SplitHTTP (XHTTP)
- **Trojan** с транспортами TCP, WS, gRPC, HTTPUpgrade, SplitHTTP (XHTTP)
- **Shadowsocks** с методами шифрования 2022-blake3-aes-128-gcm, 2022-blake3-aes-256-gcm
- Поддержка **Reality** для VLESS (TLS fingerprint, SNI, публичные ключи)
- Поддержка **XTLS-RPRX-Vision** flow для прямого TLS
- Балансировка нагрузки между несколькими прокси-серверами
- Мониторинг здоровья серверов через Observatory с автоматическим переключением
- Импорт конфигураций из share-ссылок и URL подписок

### Веб-интерфейс управления
- Визуальное управление политиками маршрутизации без терминала
- JSON редактор с подсветкой синтаксиса и валидацией для xRay конфигураций
- Загрузка доменов из GitHub репозиториев и по прямым ссылкам
- Интеграция с iplist порталом
- Валидация доменов и обнаружение конфликтов
- Экспорт/импорт конфигураций в CSV формате

---

## 📋 Системные требования

- Роутер Keenetic с прошивкой выше v.4.3.6 и установленным [Entware](https://help.keenetic.com/hc/ru/articles/360021214160)

---

## 💾 Установка и обновление

Выполните команду в терминале:
  ```bash
  opkg update && opkg install curl && curl -Ls "https://ground-zerro.github.io/release/keenetic/install-neo.sh" | sh
  ```

<details>
<summary>Вариант установки вручную</summary>

  1. Установите `curl`, если не установлен:
  ```bash
  opkg install curl
  ```
  2. Добавьте репозиторий:
  ```bash
  curl -Ls "https://ground-zerro.github.io/release/keenetic/install-feed.sh" | sh
  ```
  3. Установите Neo:
  ```bash
  opkg install hrneo
  ```

  > HydraRoute Neo готов к работе сразу после установки. Служба запускается автоматически.

</details>

🔁 Обновление производится стандартной командой:
```bash
opkg update && opkg upgrade
```
> Файлы `domain.conf` и `ip.list` сохраняются — они входят в conffiles.

---

## 📁 Работа с доменами

### Через Web-интерфейс

**HydraRoute Neo WebUI** - веб-приложение для управления маршрутизацией трафика. Позволяет настраивать политики маршрутизации для доменов и CIDR сетей, управлять xRay прокси-интерфейсами и службой HR Neo.

#### Установка

```bash
opkg install hrweb
```

> После запуска веб-интерфейс доступен по адресу: `http://192.168.1.1:2000`  
> Авторизация через логин/пароль роутера

<details>
<summary>Подробнее о возможностях HydraRoute Neo WebUI</summary>

### Dashboard (Панель управления)
- Управление политиками маршрутизации (обычные и интерфейсные)
- Добавление доменов и CIDR сетей для каждой политики
- Загрузка доменов из GitHub репозиториев
- Загрузка доменов по прямой ссылке
- Интеграция с iplist порталом и github
- Загрузка списка доменов и CIDR по прямой ссылке
- Валидация доменов и CIDR
- Обнаружение конфликтов между политиками
- Реактивное обновление интерфейса без перезагрузки страницы

### Proxy (xRay)
- Установка xRay прокси-сервера
- Управление состоянием службы (запуск/остановка)
- Создание прокси-интерфейсов через Builder (vless://, vmess://, trojan://, ss://)
- Редактирование конфигурации через JSON с подсветкой синтаксиса (CodeMirror)
- Удаление пользовательских интерфейсов
- Отображение статуса xRay-прокси интерфейсов

### DANGER ZONE (HrNeo)
- Управление службой HrNeo (запуск/остановка)
- Настройка путей к конфигурационным файлам
- Управление параметрами ipset (timeout, clear)
- Включение/отключение CIDR маршрутизации
- Включение/выключение NR Neo
- Настройка логирования (syslog/файл)
- Реактивное отображение статуса службы

## Технический стек

### Backend
- Язык: Go 1.25.4
- Веб-сервер: Встроенный HTTP сервер
- Статические файлы: Встроены в бинарник через `embed`

### Frontend
- **Архитектура:** SPA (Single Page Application) без фреймворков
- **Реактивность:** Собственная система StateManager + UIBindings
- **Computed Values:** 13 вычисляемых значений для производных данных
- **Редакторы:** CodeMirror для JSON, встроенный редактор как fallback
- **Валидация:** Web Workers для асинхронной валидации доменов
- **Общий объем:** ~7041 строка JavaScript кода

### Безопасность
- Авторизация через логин/пароль
- Cookie-based сессии
- CORS прокси для безопасных внешних запросов
- Валидация входных данных
- Sanitization доменов перед сохранением

### Архитектурные особенности
- Модульная структура (IIFE паттерн для инкапсуляции)
- Реактивная система с автоматическим отслеживанием зависимостей
- Система computed values с кэшированием результатов
- Композитные computed values (зависящие от других computed)
- Snapshot система для отката изменений
- Debounce для оптимизации производительности
- Кэширование валидации и санитизации данных

### UX
- SPA поведение без перезагрузки страницы
- Мгновенное обновление интерфейса
- Реактивное отображение статистики
- Плавные переходы между состояниями

### Оптимизации фронтенда
- Реактивные привязки для автоматического обновления UI
- 13 computed values с автоматическим кэшированием
- Композитные computed values для сложной логики
- Debounce для оптимизации частых операций
- Кэширование результатов валидации и санитизации
- Web Workers для асинхронной обработки

</details>

### Также управление доменами доступно в [**web4static**](https://github.com/spatiumstas/web4static)  
> 💡 Благодарность разработчику: [spatiumstas](https://github.com/spatiumstas)

### Вручную через терминал

<details>
<summary>Подробнее о ручном редактировании</summary>

Редактируйте файл `/opt/etc/HydraRoute/domain.conf`:
```bash
nano /opt/etc/HydraRoute/domain.conf
```

Пример:
```text
youtube.com,googlevideo.com/PolicyName1
openai.com,chatgpt.com/PolicyName2
2ip.ru,2ip.io/PolicyName3
```

> 💡 Домены разделяются **`,`** (запятой), имя политики указывается после слеша **`/`**, пробелы в строках **не допускаются**

CIDR диапазоны редактируются в `/opt/etc/HydraRoute/ip.list`:
```text
##IP для PolicyName1
/PolicyName1
111.111.111.111/32
222.222.222.0/24
2606:4700:1234:abcd::/56

##IP для PolicyName2
/PolicyName2
123.123.123.123/32
198.51.100.0/24
2a00:1450:4001:820::/64

##Отключенный блок
#/PolicyName3
192.168.1.0/24
```

> Группы отделяются пустыми строками и/или комментарием.  
> Включенные группы CIDR начинаются с `/`, отключенные с `#/`, комментарии с `##`.

Количество отдельных блоков для одной Политики не ограничено, пример:
```text
##Google
/PolicyName1
10.203.14.12/28

##YouTube (временно отключено)
#/PolicyName1
172.22.48.77/26
173.23.49.78/26

##Discord
/PolicyName1
192.168.143.19/28
```

⚠️ **Избегайте** повторения доменов, IP и перекрывающих друг друга подсетей в разных политиках  
⚠️ **Строго не рекомендуется** добавление более 45 000 строк для одной политики

> 💡 Для перенаправления отдельных доменов и CIDR в разные туннели, добавьте для них отдельные строки в `domain.conf` и `ip.list`

</details>

---

## 🔧 Политики доступа

- Создаются автоматически при запуске Neo или по `neo restart`.
- Если политика удалена и в ней есть домены — она пересоздаётся.
- Не требуется добавлять устройства в политику Neo.
- При остановке Neo (`neo stop`) трафик идёт напрямую без туннелей.

> Приоритет маршрутизации задаётся в интерфейсе Keenetic: **Приоритеты подключений → Политики доступа**.  
> Если туннель, указанный в `Политике доступа` или `Интерфейс` не доступны — трафик доменов блокируется.

В одной политике можно указать несколько VPN-подключений одновременно, активировав [режим многопутевой маршрутизации Keenetic](https://help.keenetic.com/hc/ru/articles/7490633500572).

---

## ⚙️ Технические детали

**Файлы конфигурации:**
- `/opt/etc/HydraRoute/hrneo.conf` — основная конфигурация.
- `/opt/etc/HydraRoute/domain.conf` — список доменов и политик.
- `/opt/etc/HydraRoute/ip.list` — CIDR диапазоны IP.

<details>
<summary>Параметры hrneo.conf</summary>

Основные параметры:
- `autoStart=true` - запуск программы (`true` - разрешить, `false` - запретить)
- `watchlistPath=/opt/etc/HydraRoute/domain.conf` - путь к файлу с доменами и интерфейсами
- `clearIPSet=true` - очищать содержимое ipset при запуске (`true` - очищать, `false` - сохранять)

CIDR функционал:
- `CIDR=false` - включить поддержку статических IP/подсетей (`true` - включить, `false` - выключить)
- `CIDRfile=/opt/etc/HydraRoute/ip.list` - путь к файлу с CIDR записями

IPSet параметры:
- `IpsetEnableTimeout=true` - автоматическое удаление записей по таймауту (`true` - включить, `false` - выключить)
- `IpsetTimeout=21600` - таймаут в секундах (21600 = 6 часов, 86400 = 24 часа)

Логирование:
- `log=console` - режим логирования (`console` - вывод в консоль, `file` - в файл, `false` - отключено)
- `logfile=/opt/var/log/LOGhrneo.log` - путь к файлу логов

> Включать лог без целей отладки **не рекомендуется**.

Функционал Direct Routing:
- `DirectRouteEnabled=true` - включить маршрутизацию трафика напрямую на сетевые интерфейсы
- `InterfaceFwMarkStart=4097` - начальный fwmark для интерфейсов (12289 = 0x3001 hex, диапазон 0x3001-0x3FFF)
- `InterfaceTableStart=101` - начальный номер таблицы маршрутизации для интерфейсов
- `InterfaceMissingBehavior=relaxed` - поведение при отсутствии интерфейса (`relaxed` - продолжить работу, `strict` - завершить с ошибкой)

Конфигурация по умолчанию:
```
watchlistPath=/opt/etc/HydraRoute/domain.conf
clearIPSet=true
IpsetEnableTimeout=true
IpsetTimeout=21600
CIDR=true
CIDRfile=/opt/etc/HydraRoute/ip.list
DirectRouteEnabled=true
autoStart=true
log=false
logfile=/opt/var/log/LOGhrneo.log
```

</details>

---

## 🔄 Управление

```bash
neo start     # Запуск HydraRoute Neo
neo stop      # Остановка и очистка iptables
neo restart   # Перезапуск с пересозданием политик
neo status    # Проверка состояния службы
```

---

## 🌐 IPv6

Если не используете — отключите IPv6 в настройках подключения провайдера и/или VPN соединения.

Для работы IPv6 через VPN необходимо соблюдение всех условий одновременно:
- IPv6 должен быть у основного провайдера
- IPv6 должен быть у VPN сервера
- IPv6 должен быть у WireGuard (PPTP, L2TP, OpenVPN etc.) пира
- IPv6 маршрутизация должна быть настроена на VPS

---

## ❌ Удаление

Стандартно:
```bash
opkg remove hrneo
```

Полное удаление (в т.ч. файлы, логи etc.) с откатом всех изменений в системе:
```bash
curl -Ls "https://ground-zerro.github.io/release/keenetic/hr-uninstall.sh" | sh
```
> ⚠️ Будут полностью удалены следующие пакеты: hrneo, hrweb, ipset, iptables, jq, hydraroute, adguardhome-go, node, node-npm

---

## ℹ️ Примечания и лицензия

HydraRoute Neo распространяется бесплатно, «как есть». Автор не несёт ответственности за последствия использования.

**Сказать "Спасибо" можно на** [Boosty](https://boosty.to/ground_zerro) ❤️